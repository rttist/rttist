{
	"name": "dev-wasm-swc",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"prepare": "pnpx @rttist/typegen@proto generate -f",
		"build": "swc src -d dist --strip-leading-paths",
		"start": "pnpm exec typegen generate -f && swc src -d dist --strip-leading-paths && node dist/index.js"
	},
	"keywords": [],
	"author": "",
	"license": "MIT",
	"dependencies": {
		"rttist": "workspace:*"
	},
	"devDependencies": {
		"@rttist/typegen": "workspace:*",
		"@swc/cli": "^0.3.9",
		"@swc/core": "1.3.106",
		"swc-plugin-rttist": "workspace:*"
	}
}
