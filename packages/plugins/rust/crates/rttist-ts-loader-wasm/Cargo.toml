[package]
name = "rttist_ts_loader_wasm"
version = "0.1.0"
edition = "2021"

[lib]
name = "rttist_ts_loader_wasm"
crate-type = ["cdylib"]

#[profile.release]
#lto = true

[dependencies]
serde = "1"
swc = "0.265.*"
swc_core = { version = "0.82.*", features = ["ecma_plugin_transform"] }
swc_ecma_parser = "0.139.1"
rttist_swc_transformer = { path = "../rttist-swc-transformer" }
wasm-bindgen = { version = "0.2.82", features = ["enable-interning"] }

# .cargo/config defines few alias to build plugin.
# cargo build-wasi generates wasm-wasi32 binary
# cargo build-wasm32 generates wasm32-unknown-unknown binary.